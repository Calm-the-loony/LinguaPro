# Домен
DOMAIN=d24.clv-digital.tech

# Порты (основные 80/443 заняты, используем 8080/8443)
HTTP_PORT=8080
HTTPS_PORT=8443

# Пароль root для MySQL (создаётся автоматически в Docker)
DB_PASSWORD=lingua_secret

# Node.js server (всё остальное настраивается через docker-compose)
NODE_ENV=production
